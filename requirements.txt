# Streamlit Community Cloud runtime dependencies ONLY (the deployed dashboard).
# The full analysis / ML / dev environment is defined in pyproject.toml -> use `uv sync`.
pandas>=2.0
numpy
scipy>=1.11
plotly>=5.18
streamlit>=1.30
